# This workflow will do a clean install of node dependencies, build the source code and run tests across different versions of node
# For more information see: https://help.github.com/actions/language-and-framework-guides/using-nodejs-with-github-actions

name: Node.js CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Use Node.js 16 🟩
        uses: actions/setup-node@v1
        with:
          node-version: 16

      - name: Install dependencies 📥
        run: npm install

      - name: Lint ✏️
        run: npm run lint

      - name: Build 🔧
        run: npm run build-ghpages

      - name: 💾 Archive austen-stone artifacts
        uses: actions/upload-artifact@v2
        with:
          name: dist
          path: dist/austen-stone

  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: ⬇️ Download austen-stone artifacts
        uses: actions/download-artifact@v2
        with:
          name: dist

      - name: 404 GitHub Pages 💫
        run: cp dist/austen-stone/index.html dist/austen-stone/404.html

      - name: Deploy to GitHub Pages 🚀
        uses: crazy-max/ghaction-github-pages@v2
        if: github.actor != 'dependabot[bot]' && github.actor != 'dependabot-preview[bot]'
        with:
          target_branch: gh-pages
          build_dir: dist/austen-stone
          fqdn: austen.info
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
